# Frontend Agent Instructions

- Match existing UI patterns, routing, state management, and API client conventions.
- Keep API contracts visible at the boundary and handle loading, empty, and error states.
- Avoid layout shifts by defining stable dimensions for repeated controls and message surfaces.
- Keep user-facing text concise and domain-specific.
- Verify responsive behavior for the primary workflow.
